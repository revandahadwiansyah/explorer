Each part must be installed and run **separately**.

---

## 🗄️ Database Setup

### 1️⃣ Add Database Using Explorer

1. Open your preferred **database explorer** (e.g. DBeaver).
2. Create a **new database connection**.
3. Create a **new database/schema** (name as required by your backend config).
4. Ensure the database is running and accessible.

---

### 2️⃣ Run Database Queries (DDL)

The database schema is provided in:

#### Steps:

1. Open your database explorer.
2. Open the file:

3. Copy or run the SQL queries inside the file.
4. Verify that tables are created successfully.

> ✅ Once completed, your database is ready to be used by the backend.

---

## 🔧 Backend Setup

### Install Dependencies

```bash
npm install
npm run dev
```
